# 词库开发索引

> **命名说明（2026-09-14）**：界面上「词库」已改称「集词阁」。本索引与各设计文档继续用"词库"作为概念名与代码名（`lexicon`），二者指同一功能。
> **用途**：把“词库”功能相关的文档、脚本、代码、数据产物集中到一处，避免在多份文件之间迷路。
> **维护约定**：每完成一个阶段，在文末「更新记录」追加一行，并在对应分类下补上新文件。
> **链接说明**：以下路径均相对项目根目录 `Egangnal/`，点击即可在编辑器或文件管理器中打开。

## 1. 现在到哪一步了

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| 5.0 | 决策与数据基线（ADR + 设计 + 构建契约） | ✅ 已完成（2026-09-11） |
| 5.1 | 数据勘察与构建工具 | ✅ 已完成（2026-09-11） |
| 5.2 | Swift 只读查询层 | ✅ 已完成（2026-09-11） |
| 5.3 | 词库界面（浏览 / 搜索 / 遮盖 / 悬停浮层） | ✅ 已完成（2026-09-11） |
| 5.3.1 | 词库体验修正（排版 / 筛选 / 洗牌 / 遮盖 / 收录释义） | ✅ 已完成（2026-09-11） |
| 5.3.2 | 词库二次修正（网格对齐 / 收藏按钮 / 浮层触发 / 收藏计入当日 + 取消收藏） | ✅ 已完成（2026-09-11） |
| 5.3.3 | 浮层缺陷修复（首次进入不显示 / 淡入突兀） | ✅ 已完成（2026-09-11） |
| 5.3.4 | 单词本盲盒 + 加功能栏改名换位 + 收藏配色与浮层日期 | ✅ 已完成（2026-09-11） |
| 5.4 | 收录闭环（词库 → 单词本） | ✅ 已完成（2026-09-11） |
| 5.5 | 词库与单词刷联动 | ✅ 已完成（2026-09-13） |
| 5.5.1 | 等级速测（单词刷里选「词库」范围 + 等级） | ✅ 已完成（2026-09-13，含 v2 返工） |
| 5.6 | 只读资料空间（原阶段 5 内容） | ⬜ 未开始，优先级待定 |

**收藏规则已于 2026-09-11 修订**：收藏现在会计入**当日**生词（出现在单词本的今天分组），并且可以再次点击取消收藏——取消时只移除词库收藏自己的记录，文档导入的词条与日期绝不受影响。详见 `ADR-003` 决策 4。

**一句话现状**：**词库功能已经完整可用，并且与单词刷接通**——在英语空间的「词库」里可以按六级/四级/高中/初中浏览、搜英文或中文、点击遮盖单词或词义、悬停看音标和完整释义；悬停词块后点 ⊕ 即可把生词收进个人单词本，已收录的词显示为浅黄色。**收藏的词会直接进入单词刷的默认抽题范围**，取消收藏后自动退出；同一题内不会再出现两个看起来一样的选项。浏览顺序每次进入会自动换一批（也可手动「换一批」）。7,116 条词条（2.5 MB）随 App 打包，单元测试 237 个用例全部通过（界面套件结果见本文第 6.3 节「测试现状」，其中两个既有抖动与代码无关）。**单词刷里还能把「词库」选为考核范围**：范围选择器是 默认 / 日期 / 词库 三项，选「词库」后可再选等级（四级 3,849 条），干扰项从整个词库抽，选项**带词性**（`n. 苹果, 家伙…`）。剩下的候选工作只有 5.6 只读资料空间。

## 2. 推荐阅读顺序（按目的选一条，不用全读）

| 你的目的 | 按顺序读 | 预计耗时 |
| --- | --- | --- |
| 先搞懂“词库要做成什么样” | [词库实现设计](docs/design/词库实现设计.md) 第 1–3 节 | 10 分钟 |
| 想知道“为什么要这么设计” | [ADR-003](docs/architecture/架构决策-003-公共词库与个人单词本分离.md) | 5 分钟 |
| 想自己跑一遍构建 | [构建工具使用说明](tools/lexicon-builder/README.md) | 5 分钟 |
| 想了解数据来源与质量 | [ECDICT 数据勘察报告](docs/design/ECDICT数据勘察报告.md) | 10 分钟 |
| 想查规则细节（schema、门槛、版本） | [ECDICT 离线词库构建契约](docs/design/ECDICT离线词库构建契约.md) | 15 分钟 |
| 想接着改代码 | 本索引第 3 节 **C 组**（Swift 查询层） | 15 分钟 |
| 想知道整体计划与下一步 | [开发路线图](docs/开发路线图.md) 阶段 5 | 5 分钟 |
| 想了解项目整体技术分层 | [项目技术与代码导览](docs/项目技术与代码导览.md) | 20 分钟 |
| 想知道代码该怎么写、活该怎么干 | [代码规范与开发准则](docs/代码规范与开发准则.md) | 15 分钟 |

## 3. 文件清单

### A. 决策与规范（先读这两份）

| 文件 | 作用 | 入库 |
| --- | --- | --- |
| [架构决策-003：公共词库与个人单词本分离](docs/architecture/架构决策-003-公共词库与个人单词本分离.md) | 6 条核心决策：公共只读词库与个人可写单词本物理分离、不上服务端、收藏不写出现记录、确定性 UUID、只读参数绑定 | ✅ |
| [ECDICT 离线词库构建契约](docs/design/ECDICT离线词库构建契约.md) | **规范性契约**：来源清单、许可证处置、等级映射、筛选规则、SQLite schema、质量门槛、版本策略。与教程冲突时以它为准 | ✅ |

### B. 设计与勘察

| 文件 | 作用 | 入库 |
| --- | --- | --- |
| [词库实现设计](docs/design/词库实现设计.md) | **功能设计总纲**：范围、字段映射、遮罩与搜索、收录规则、与单词刷联动、界面、容量、接口边界、风险、测试验收、5.0–5.6 拆分 | ✅ |
| [ECDICT 数据勘察报告](docs/design/ECDICT数据勘察报告.md) | 真实数据实测结论：7,116 条、`pos` 列 100% 为空、字面 `\n`、词性前缀 99.62%、零重复零碰撞，以及对设计的 8 条确定性影响（D1–D8） | ✅ |
| [词库界面设计](docs/design/词库界面设计.md) | **5.3 界面规格**：与单词本的一致/差异点、双栏词块、检索区、悬停浮层（音标+完整释义+等级）、遮盖、50 条分页、字号常量、无障碍标识、验收标准、5.3/5.4 边界 | ✅ |
| [词库与单词刷联动实现设计](docs/design/词库与单词刷联动实现设计.md) | **5.5 依据**：现状核实（收藏词早在抽题池）、显示文本冲突的实测规模与算法、等级速测为何后置、验收标准与后续评估项 | ✅ |
| [等级速测实现设计](docs/design/等级速测实现设计.md) | **5.5.1 依据**：三个遗留约束的解法、范围与选项文本口径、行为规格、测试计划、验收记录与三个自查缺陷 | ✅ |

### C. Swift 业务代码（5.2 查询层 + 5.3 界面）

| 文件 | 作用 | 入库 |
| --- | --- | --- |
| [Egangnal/Domain/LexiconModels.swift](Egangnal/Domain/LexiconModels.swift) | 等级枚举（由高到低）、词条快照、查询意图（含**洗牌种子**）、分页结果、**检索模式判定** | ✅ |
| [Egangnal/Stores/ShuffleSeedController.swift](Egangnal/Stores/ShuffleSeedController.swift) | 洗牌时机：15 分钟冷却 + 手动换一批；词库与单词本各持一个实例 | ✅ |
| [Egangnal/Design/EntryFieldsLayout.swift](Egangnal/Design/EntryFieldsLayout.swift) | 单词本与词库共用的字段排布（原 `EqualFieldsLayout`，2026-09-13 起支持宽度比与基线对齐） | ✅ |
| [Egangnal/Domain/LexiconGlossFormatter.swift](Egangnal/Domain/LexiconGlossFormatter.swift) | 短释义派生：`shortGloss`（单词刷，12 字符）与 `browsingDisplay`（词库列表，保留词性前缀） | ✅ |
| [Egangnal/Data/LexiconRepository.swift](Egangnal/Data/LexiconRepository.swift) | `LexiconRepository` 协议 + 系统 `libsqlite3` **只读**实现（英文前缀范围查询 + 中文 `LIKE` 检索）+ 不可用/预览实现 | ✅ |
| [Egangnal/Features/LearningMaterials/LexiconCatalogStore.swift](Egangnal/Features/LearningMaterials/LexiconCatalogStore.swift) | 页面状态：页码、等级、关键词、遮盖、加载与错误 | ✅ |
| [Egangnal/Features/LearningMaterials/LexiconView.swift](Egangnal/Features/LearningMaterials/LexiconView.swift) | 页面组合、悬停延迟与浮层定位（含边缘翻转） | ✅ |
| [Egangnal/Features/LearningMaterials/LexiconComponents.swift](Egangnal/Features/LearningMaterials/LexiconComponents.swift) | 标题行、检索区、词块（高亮 + 遮盖 + 收录按钮）、悬停浮层、分页、空状态 | ✅ |
| [EgangnalTests/LexiconRepositoryTests.swift](EgangnalTests/LexiconRepositoryTests.swift) | 查询层测试：等级数量、分页边界、英文/中文检索、参数绑定、错误路径 | ✅ |
| [EgangnalTests/LexiconNormalizationTests.swift](EgangnalTests/LexiconNormalizationTests.swift) | **跨语言校验**：18 条固定样例 + 全部 7,116 条真实词条的查词键一致性 | ✅ |
| [EgangnalTests/LexiconGlossFormatterTests.swift](EgangnalTests/LexiconGlossFormatterTests.swift) | 短释义与浏览级释义测试：`a.` 前缀、`[计]` 标注、语法说明行、截断与省略号 | ✅ |
| [EgangnalTests/LexiconCatalogStoreTests.swift](EgangnalTests/LexiconCatalogStoreTests.swift) | Store 行为：查询下发、翻页边界、遮盖隔离与清除、失败与空结果区分 | ✅ |
| [EgangnalTests/WordEntrySourceTests.swift](EgangnalTests/WordEntrySourceTests.swift) | **来源迁移与收藏**：回填前后判定、幂等、不覆盖释义、**计入当日**、跨语言隔离、**取消收藏不损坏文档导入**；5.5 新增**收藏 → 进入单词刷抽题池 → 取消收藏 → 退出**的端到端用例 | ✅ |
| [EgangnalTests/LexiconTestSupport.swift](EgangnalTests/LexiconTestSupport.swift) | 测试共用的路径解析与轻量 SQLite 读写 | ✅ |

### C3. 5.5 改动的既有文件

| 文件 | 改了什么 |
| --- | --- |
| [Egangnal/Domain/WordQuizEngine.swift](Egangnal/Domain/WordQuizEngine.swift) | 新增 `displayText(of:)` 与 `acceptDistractor`：干扰项挑选多一道"显示文本不得撞车"的闸门；**领域契约（题目结构 / 评分 / 错题重做 / 难度分支）不变** |
| [EgangnalTests/WordQuizEngineTests.swift](EgangnalTests/WordQuizEngineTests.swift) | 新增 3 条用例：近义干扰项不撞车（20 个随机种子）、凑不齐选项时明确失败、显示文本折叠空白规则 |
| [EgangnalTests/LexiconGlossFormatterTests.swift](EgangnalTests/LexiconGlossFormatterTests.swift) | 新增 2 条全库用例：7,116 条"引擎判定文本 = 界面显示文本"、冲突规模基线（93 组 / 189 条 / 684 条触上限） |

> 5.5 **没有新增 App 代码文件**，也没有改动候选层（`WordQuizCandidateSource`）、`WordQuizStore` 与任何界面文件。

### C4. 5.5.1 改动的既有文件与新文件（含 v2 返工）

| 文件 | 改了什么 |
| --- | --- |
| [Egangnal/Domain/WordQuizModels.swift](Egangnal/Domain/WordQuizModels.swift) | 新增 `LexiconQuizScope`（`.all` / `.level`，**筛选描述**，不携带词条）；`WordQuizScope` 新增 `.lexicon` 分支 |
| [Egangnal/Data/LexiconRepository.swift](Egangnal/Data/LexiconRepository.swift) | 协议新增**不分页入口** `entries(matching:)`；WHERE 子句与绑定抽成 `filterClause` / `filterBindings` 供两条路径共用 |
| [Egangnal/Data/WordQuizCandidateSource.swift](Egangnal/Data/WordQuizCandidateSource.swift) | 新增 `LexiconQuizCandidateProviding` 与 `LexiconQuizCandidateSource`：目标集 = 等级筛选结果，干扰项池 = 整个词库，释义用 `browsingDisplay` 派生（**带词性**） |
| [Egangnal/Stores/WordQuizStore.swift](Egangnal/Stores/WordQuizStore.swift) | `WordQuizRangeMode` 新增 `.lexicon`；`lexiconLevel` 与等级选择；两套独立候选池；空范围与读取失败的**区分**提示 |
| [Egangnal/Features/WordQuiz/WordQuizView.swift](Egangnal/Features/WordQuiz/WordQuizView.swift) | 范围选择器加「词库」；词库范围下把日期选择器换成**等级选择器** |
| [Egangnal/ContentView.swift](Egangnal/ContentView.swift) | 只负责组装词库候选源（v2 后不再传范围） |
| [EgangnalTests/LexiconQuizCandidateSourceTests.swift](EgangnalTests/LexiconQuizCandidateSourceTests.swift) | **新文件**：范围到查询的映射、释义派生与词性、真实词库端到端组卷、错误传播 |
| [EgangnalUITests/EgangnalUITests.swift](EgangnalUITests/EgangnalUITests.swift) | 新增 1 条界面验收：范围三项、等级选择器出现、未收藏的词也能考、选项带词性 |

> **v2 撤销的改动**：词库页的「开始单词刷」入口（`LexiconView` / `LexiconComponents` 已回到 5.4 状态）、
> `ContentView` 跨工作区传范围的 `@State` 与"导航时清空范围"的纯函数、
> 以及 `EgangnalTests/LexiconQuizNavigationTests.swift`（已删除）。
>
> 5.5.1 **没有改动** `WordQuizEngine`、`WordBookRepository` 与个人单词本读写；
> 速测全程不产生任何 SwiftData 写入。

### C2. 5.4 改动的既有文件

| 文件 | 改了什么 |
| --- | --- |
| [Egangnal/Domain/WordBookModels.swift](Egangnal/Domain/WordBookModels.swift) | 新增 `WordEntrySource` 与 `WordBookCollectionOutcome`；`WordEntry` 旧布尔字段重命名为 `legacyIsManuallyCreated` 并新增 `sourceRawValue`，`source` 为唯一判定入口 |
| [Egangnal/Data/WordBookRepository.swift](Egangnal/Data/WordBookRepository.swift) | 新增 `collectFromLexicon`（幂等、不覆盖释义、不产生出现记录）与 `migrateLegacyEntrySourcesIfNeeded` |
| [Egangnal/App/AppDependencies.swift](Egangnal/App/AppDependencies.swift) | 启动时执行来源回填；失败时记录原因并退回旧字段推断，不阻塞启动 |
| [Egangnal/Design/AppTheme.swift](Egangnal/Design/AppTheme.swift) | 新增 `collectedAccent`（浅色深金 / 深色浅黄） |
| [Egangnal/Features/WordBook/WordBookView.swift](Egangnal/Features/WordBook/WordBookView.swift) | “未归档单词”副标题改为“手动新增或词库收藏 N 个” |
| [EgangnalUITests/LexiconUITests.swift](EgangnalUITests/LexiconUITests.swift) | **界面验收**：等级筛选与基线数量、英文/中文搜索、遮盖、分页 | ✅ |

### D. 构建工具与脚本（`tools/lexicon-builder/`）

| 文件 | 作用 | 入库 |
| --- | --- | --- |
| [README.md](tools/lexicon-builder/README.md) | **使用说明**：三步上手、数据丢失后如何重新取得、参数表、可复现性验证、报错对照表、如何扩展等级 | ✅ |
| [build_ecdict.py](tools/lexicon-builder/build_ecdict.py) | **构建工具**：来源校验 → 清洗 → 换行还原 → 建库 → 质量门槛 → 原子替换。仅标准库，含 `--self-test` | ✅ |
| [survey_ecdict.py](tools/lexicon-builder/survey_ecdict.py) | **只读勘察脚本**：统计字段缺失率、标签分布、释义形态、体积估算 | ✅ |
| [fixtures/normalization-cases.json](tools/lexicon-builder/fixtures/normalization-cases.json) | 18 条跨语言规范化固定样例，Python 与 Swift **共用同一份**（Swift 测试直接读取） | ✅ |
| [output/build-report.json](tools/lexicon-builder/output/build-report.json) | 本次构建全部统计与 10 项质量门槛结果 | ✅ |
| [output/source-manifest.json](tools/lexicon-builder/output/source-manifest.json) | 本次实际使用的来源身份（提交号与摘要） | ✅ |

### E. 数据产物

| 文件 | 作用 | 入库 |
| --- | --- | --- |
| [Egangnal/Resources/Lexicon/EgangnalLexicon.sqlite](Egangnal/Resources/Lexicon/EgangnalLexicon.sqlite) | **只读词库本身**：7,116 条，2.5 MB，已自动打包进 App | ✅ |
| [Egangnal/Resources/Licenses/ECDICT-LICENSE.txt](Egangnal/Resources/Licenses/ECDICT-LICENSE.txt) | ECDICT 的 MIT 许可证原文（署名义务，公开分发时随包提供） | ✅ |

### F. 参考教程（**会话开始前就存在**，不是本次产出）

| 文件 | 作用 |
| --- | --- |
| [ECDICT 接入保姆级教程](docs/ECDICT接入保姆级教程.md) | 1,290 行操作手册：从下载、CSV 分析到 Swift 查询层的完整走法。**遇到不懂的步骤先查它** |
| [WordNet 接入保姆级教程](docs/WordNet接入保姆级教程.md) | WordNet 方案草案（多义项、同义词、语义关系），**本阶段不采用**，属后续评估 |

### G. 本会话修改过的既有文件

| 文件 | 改了什么 |
| --- | --- |
| [docs/开发路线图.md](docs/开发路线图.md) | 阶段 5 重构为「学习资料——词库」，新增 5.0–5.6 子阶段与验收标准；原「只读资料空间」保留为 5.6 |
| [.gitignore](.gitignore) | 新增 `.lexicon-source/`，避免 63 MB 原始数据误入仓库 |
| [README.md](README.md) | 「项目文档」中加入本索引、ADR-003、词库实现设计、代码规范与开发准则 |
| [docs/开发规范.md](docs/开发规范.md) | 头部加入与《代码规范与开发准则》的关系说明和优先级约定 |
| [docs/代码规范与开发准则.md](docs/代码规范与开发准则.md) | 新建：工程约定总纲（本文第 6.4 节的完整展开） |

### H. 不进仓库的临时输入（`.lexicon-source/`，63 MB）

这些是**可重新取得的构建输入**，已在 `.gitignore` 中。丢失后按 [构建工具使用说明](tools/lexicon-builder/README.md) 第 4 节重新下载即可。

| 文件 | 作用 |
| --- | --- |
| `ecdict.csv`（63 MB） | ECDICT 基础版词库原始数据（76 万词条） |
| `ecdict.mini.csv` | 小样本，供工具早期测试 |
| `LICENSE` / `README.md` | 来源许可证与说明，用于摘要校验 |
| `source-manifest.json` | 来源清单（构建工具会校验它） |
| `survey-report.json` | 勘察脚本的机器可读输出 |

**保留决定（2026-09-11）**：暂时**保留**这 63 MB，等词库功能（5.3–5.5）做完、数据结构稳定后再清理。原因：中途若需要调整词库结构，有原始数据在手就不必重新下载。它**不会**进入 Git，也**不会**进入 App。

> 澄清一个常见误解：App 里**只有筛选后的 7,116 个词**（2.5 MB）。其余 75 万多个词在 5.1 加工时就被筛掉了，从未进入 App，运行时也不会被访问。

## 4. 常用命令速查

```bash
# —— 词库构建（开发期，不进 App）——

# 快速自检（秒级，不需要源文件）
python3 tools/lexicon-builder/build_ecdict.py --self-test

# 完整构建词库
python3 tools/lexicon-builder/build_ecdict.py

# 验证可复现性（两次构建字节应一致）
python3 tools/lexicon-builder/build_ecdict.py --generated-at 2026-09-11T00:00:00Z

# 查一个词
sqlite3 Egangnal/Resources/Lexicon/EgangnalLexicon.sqlite \
  "SELECT term, phonetic, chinese_translation FROM dictionary_entry WHERE term='abandon';"

# —— App 构建与测试 ——

# 构建
xcodebuild build -project Egangnal.xcodeproj -scheme Egangnal \
  -configuration Debug -destination 'platform=macOS'

# 单元测试（含词库查询层测试）
xcodebuild test -project Egangnal.xcodeproj -scheme Egangnal \
  -configuration Debug -destination 'platform=macOS' -only-testing:EgangnalTests
```

## 5. 更新记录

| 日期 | 阶段 | 新增/变更 |
| --- | --- | --- |
| 2026-09-11 | 5.0 | ADR-003、词库实现设计、ECDICT 离线词库构建契约；路线图新增阶段 5.0–5.6 |
| 2026-09-11 | 5.1 | 数据勘察报告、survey_ecdict.py、build_ecdict.py、README、固定样例、词库 SQLite、许可证文件、构建报告 |
| 2026-09-11 | 5.2 | LexiconModels.swift、LexiconGlossFormatter.swift、LexiconRepository.swift + 4 个测试文件（新增 29 个测试，全部 118/118 通过） |
| 2026-09-11 | 5.3 设计 | 词库界面设计（含悬停浮层方案）；修正每页 20 条为 50 条；确认支持中文释义检索（实测全表扫描 1–2 ms，无需索引） |
| 2026-09-11 | — | 澄清并修正文档：**一个词只能收藏一次**（按钮是二元开关），"跨日收藏成为高频词"界面做不到，仅在仓储层作为异常路径的自洽能力保留 |
| 2026-09-11 | 5.3.4 | 单词本接入盲盒（独立控制器）；顶部栏改名「词库」并与单词刷换位（`WorkspaceFeature.lexicon`）；修复已收藏单词未变色；浮层显示收藏日期 |
| 2026-09-11 | 5.3.3 | 修复首次进入浮层不显示（误清布局缓存）与淡入突兀（透明度由测量结果直接决定）；字段补 contentShape；音标补进无障碍标签 |
| 2026-09-11 | 5.3.2 | 修正网格错位与基线对齐；收藏按钮移入词条行；浮层仅由词性词义触发；**收藏计入当日并可取消收藏**（ADR-003 决策 4 修订） |
| 2026-09-11 | 5.3.1 | 词块改回左右两栏、等级改为由高到低、检索区收窄居中与渐变分隔线、种子洗牌与「换一批」、遮盖贴合文字、收录写入短释义；修正洗牌公式缺陷 |
| 2026-09-11 | 5.4 | 来源枚举迁移 + 回填、`collectFromLexicon`、悬停显形的收录按钮与已收录标记、来源迁移测试与收录界面测试 |
| 2026-09-11 | 5.3 实现 | LexiconCatalogStore / LexiconView / LexiconComponents + Store 与界面测试；中文检索与 `pageSize` 50 落地；**修复搜索框绑定缺陷**（单元测试无法覆盖，界面测试发现） |
| 2026-09-13 | 5.5 | 词库与单词刷联动实现设计；核实「收藏词早在抽题池」并补端到端测试；`WordQuizEngine` 新增显示文本冲突闸门（93 组 / 189 条词真实冲突）；等级速测后置为 5.5.1；**修正两处实测错误**（此前用错派生函数、误用 `isTruncated` 口径） |
| 2026-09-13 | 5.5.1 | 等级速测实现设计；`LexiconQuizScope` 与 `.lexicon` 范围、不分页查询入口、`LexiconQuizCandidateSource`；**v2 按用户反馈返工**：入口从词库页移到单词刷的范围选择器（撤掉整套跨工作区传参）、选项改为带词性；保留两条自查修正（失败原因被覆盖、范围文案措辞） |
| 2026-09-11 | — | 建立本索引；记录 `.lexicon-source/` 暂缓清理的决定 |

## 6. 新会话交接说明

> 这一节是给**下一个会话**看的。新会话没有之前的对话记忆，请先读完本节，再按需展开其它文档。

### 6.1 现在做到哪了

词库功能的**主体已经全部完成并对用户可用**，且已与单词刷双向接通。阶段 5.0 – 5.5.1 以及三轮体验修正（5.3.1 – 5.3.4）均已收尾，验证记录都写在《开发路线图》里。

用户现在可以：在英语空间的「词库」里按 全部 / 六级 / 四级 / 高中 / 初中 浏览（每次进入自动换一批，也可手动换）；搜英文前缀或中文释义；点击遮盖单词或词性词义；悬停词性词义看音标、完整释义、等级与收藏日期；点 ⊕ 收藏、再点取消收藏（已收藏的单词与按钮同为浅黄色）。单词本的默认分页也接入了同样的盲盒机制。

**收藏的词会直接进入单词刷的默认抽题范围，取消收藏后自动退出**（5.5 用端到端测试锁定），并且同一题内的两个选项不会再出现一模一样的文字。

**还能从词库直接发起速测**（5.5.1）：筛到某个等级或搜到某批词后点标题栏的「开始单词刷」，就只考这一批
（四级 3,849 条），干扰项从整个词库抽，全程不写个人数据。

### 6.2 下一步：只剩 5.6

词库这条线**已经全部做完**（5.0 – 5.5.1）。剩下的是 **5.6 只读资料空间**：
原阶段 5 内容（用户 `.md` / `.txt` 导入与只读阅读），拆分建议与验收标准见[开发路线图](docs/开发路线图.md)阶段 5.6。
它**不在词库范围内**，与本索引记录的数据结构基本无关，动工前**必须先写需求 / 数据结构 / 风险 / 验收标准文档**（见本文第 6.4 节第 1 条）。

若用户想继续打磨词库/单词刷，可选的小项记录在各设计文档的「后续评估项」里，例如：
速测题量是否可调、是否记录速测成绩、同一词在两条链路上的释义文本是否统一。

### 6.3 动手前必须知道的三件事

**1）所有改动都还没有提交。**
仓库里唯一的提交是 `0bfec8c chore: establish Egangnal project baseline`，之后全部改动都处于未提交状态（含词库线的全部代码、测试与文档）。
提交时机由用户决定，不要擅自提交。

**2）测试现状。**
- 单元测试：`xcodebuild test -only-testing:EgangnalTests`，**237 个用例全部通过**（词库线自身的用例约占一半，其余为计时、单词本、设置等既有功能）。
- 界面测试：`xcodebuild test -only-testing:EgangnalUITests`。**失败数随本机状态浮动**，实际观察过 26/28、27/28 与 29/30（5.5.1 新增 2 条后）三种结果，失败的都是下面两个**既有**问题，与代码无关：
  - `testWordQuizDeepModeMovesFromChoicesToSpellingAndResult`：逐键输入不稳定，提交拼写后等不到反馈元素（`wordQuiz.feedback`）。已在基线提交 `0bfec8c` 上复现过同样症状。
  - 整轮运行的**第一个**用例有时会被本机残留进程卡住（"Failed to terminate com.ly.Egangnal"超时 60 秒）。重启电脑可清除。**不是每轮都出现**：2026-09-13 的 5.5 验收轮就没有出现。
- 运行 xcodebuild 需要 `danger-full-access`：它要写 `~/Library` 下的 DerivedData 与测试结果包，工作区沙箱会拒绝。

**3）本环境无法做视觉验证。**
终端没有「屏幕录制」权限，`screencapture` 只能拍到桌面壁纸。因此所有视觉项（字号、间距、配色、浮层观感）**必须由用户人工运行 App 复核**。
用这个命令直达词库页：
```bash
open -a "$(find ~/Library/Developer/Xcode/DerivedData -name 'Egangnal.app' -type d | grep -v Index.noindex | head -1)" \
  --args --ui-testing-workspace-lexicon
```

**4）实测数字务必核对口径。**
同一个"截断"有三种数量级不同的口径（684 / 111 / 6,271），同一个"冲突"也因派生函数不同差一个数量级
（`browsingDisplay` 93 组 vs `shortGloss` 891 组）。**引用前先确认用的是哪条链路、哪个函数**——
本项目已经在这上面栽过两次，详见[词库与单词刷联动实现设计](docs/design/词库与单词刷联动实现设计.md)第 2 节与路线图 5.5 的验证记录。

### 6.4 这个项目的做事方式（用户明确要求，务必遵守）

> 完整版已整理为 [代码规范与开发准则](docs/代码规范与开发准则.md)（含分层依赖、命名风格、并发隔离、提交规范与完成定义）。下面六条是其中与本功能最相关的摘要。

1. **文档先行**：每个大阶段开始前先写需求 / 数据结构 / 风险 / 验收标准，评审通过再动代码。
2. **小步推进**：一次只解决一个清晰问题，每步可独立验证，不跨阶段提前实现。
3. **中文注释**：标识符用清晰英文，注释只解释**为什么**、边界和不变量，不做逐行翻译。
4. **低耦合**：视图不直接读 SQLite / SwiftData；外部能力用协议包裹；应用级对象只在 `AppDependencies` 创建一次。
5. **不迎合**：发现用户方案有问题要直接指出并给依据；不确定的事先实测再下结论，不要把推测当事实。
6. 行为变化时同步更新测试与对应文档；发现文档与代码不一致时，**要么改代码要么改文档**，不允许两边说法不同。

### 6.5 已知的历史坑（避免重复踩）

- 洗牌公式若写成 `hash ^ seed` 或 `id * K + seed`，种子几乎不改变顺序，等于没洗牌；正确写法是让种子参与完整散列或乘法。
- `.foregroundStyle` 是环境值，**不会传播到后加的 `.overlay` 内容**；遮盖 / 上色必须在 overlay 内部再写一次。
- 浮层的透明度不能由「尺寸是否测量完成」直接决定，否则不在动画范围内，会瞬显；要先透明渲染一帧、再由动画状态触发淡入。
- `clearHover()` 这类「状态清理」不能顺手清掉**布局缓存**（`cellFrames` / `tooltipSize`），清掉后偏好值未变、不会重新上报。
- 任何滚动锚点都不能放进 `LazyVGrid` 的子视图位置，否则会占掉第一个格位、让整列错位。
- **统计数据前先核对口径**：同一个"截断"有 684 / 111 / 6,271 三种差一个数量级的说法，"冲突"也因派生函数不同差一个数量级（`browsingDisplay` 93 组 vs `shortGloss` 891 组）。凭直觉选一个函数去估算，就会写出错误的文档和错误的测试断言——本项目已经栽过两次。
- **`isTruncated` 不是"被长度上限截断"**：它的含义是"还有更多内容可看"，多行释义（如 `n. 飞机\n[机] 飞机`）也算，因此 6,271 ≠ 684。
